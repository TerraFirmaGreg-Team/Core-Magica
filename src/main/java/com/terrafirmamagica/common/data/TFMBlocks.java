package com.terrafirmamagica.common.data;

import com.terrafirmamagica.TFMCore;
import net.neoforged.neoforge.registries.DeferredRegister;

public class TFMBlocks {
    // Create a Deferred Register to hold Blocks which will all be registered under the "tfm" namespace
    public static final DeferredRegister.Blocks BLOCKS = DeferredRegister.createBlocks(TFMCore.MOD_ID);

    public static void init() {

    }
}
