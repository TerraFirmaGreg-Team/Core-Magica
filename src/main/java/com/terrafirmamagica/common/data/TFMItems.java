package com.terrafirmamagica.common.data;

import com.terrafirmamagica.TFMCore;
import net.neoforged.neoforge.registries.DeferredRegister;

public class TFMItems {
    // Create a Deferred Register to hold Items which will all be registered under the "tfm" namespace
    public static final DeferredRegister.Items ITEMS = DeferredRegister.createItems(TFMCore.MOD_ID);

    public static void init() {

    }
}
